🛠️ Build: [improve OpenCL config parsing and logging, add hardware backend stubs]

What:
- Updated `RK_USE_OPENCL` parsing in `Engine.cpp` to support "true/yes/on" (case-insensitive).
- Stored requested OpenCL status in `VideoManager` and updated logging to show both requested and effective status.
- Added infrastructure stubs and placeholders for Qualcomm SDK and MPP hardware decoding, following the `Hardware Testing in Sandbox` directive which requires verifying logic via CPU fallbacks and leaving '待补测' placeholders.

Why:
- To correctly parse standard boolean config strings for OpenCL.
- To avoid confusion by explicitly showing whether OpenCL was requested vs. whether it is actually effective.
- To prepare the codebase for real hardware device execution of Qualcomm SDK and MPP by setting up the testing and fallback infrastructure in the sandbox.

Verification:
- Compiled successfully with `-DRK_SKIP_OPENCV=ON`.
- Core unit tests pass.

Risk & Rollback:
- Low risk. The configuration parsing and logging are straightforward string changes. The hardware backends fall back to CPU by default.
- Rollback by reverting the commit.
