# 构建系统与 CI 配置审查报告

**日期**: 2026-07-04

---

## 1. 模块概览

| 指标 | 数值 |
|:----|:----:|
| 审查文件 | 19 个 |
| 构建系统 | CMake (C++) + Gradle (Android) + Vite (Web) |
| CI 系统 | GitHub Actions (1 workflow) |
| 辅助脚本 | 12 个（bat/ps1/sh/js/py）|
| C++ 目标数 | 14+ |

---

## 2. 逐文件审查

### CMake (CMakeLists.txt 983行)

| 评分 | 评语 |
|:----:|:------|
| **2/5** | 单体文件14+目标。CORE_SOURCES 在6个目标间逐字复制(~1800行冗余)。16模块OpenCV链接列表重复7次。**致命缺陷**：line 969-979 foreach循环嵌套在 `if(WIN32)` 内，导致Android/Linux目标缺失 `RK_HAVE_MPP`/`RK_HAVE_QUALCOMM` 编译宏 |

### Gradle

| 文件 | 评分 | 评语 |
|:----|:----:|:------|
| app/build.gradle | **3/5** | 硬编码Windows路径（ncnn），ABI过滤逻辑重复 |
| settings.gradle | **4/5** | FAIL_ON_PROJECT_REPOS 防止子项目私自加仓库 |
| gradle-wrapper.properties | **2/5** | 使用 Gradle 9.0-milestone-1 预发布版 |

### Web 构建

| 文件 | 评分 | 评语 |
|:----|:----:|:------|
| vite.config.ts | **4/5** | base:'./' 相对路径兼容，outDir 与 Windows 后端共享但产生跨目录耦合 |
| tsconfig.app.json | **5/5** | strict+noUnusedLocals+erasableSyntaxOnly+noUncheckedSideEffectImports 全开 |

### CI (ci.yml)

| 评分 | 评语 |
|:----:|:------|
| **2/5** | **Windows job 在 PR 上从不触发**（line 188限定push+Windows关键字），无ccache/Gradle缓存，E2E用continue-on-error掩盖失败，120min超时暗示构建过长 |

---

## 3. Top 5 关键问题

| 严重性 | 问题 | 位置 |
|:------|:-----|:------|
| **🔴 致命** | foreach 编译宏循环被 WIN32 守卫困住 — RK_HAVE_MPP/QUALCOMM 在 Android/Linux 缺失 | CMakeLists.txt:969-979 |
| **🔴 致命** | CORE_SOURCES 在6+目标间逐字复制 — 维护负担高，无增量收益 | CMakeLists.txt:329-359 |
| **🔴 高** | OpenCV 16模块链接列表重复7次 — 增减模块需改7处 | CMakeLists.txt 多处 |
| **🔴 高** | CI Windows 不对 PR 触发 — 跨平台 PR 无法 CI 验证 | ci.yml:188 |
| **🟡 中** | 无 CI 构建缓存(ccache/Gradle) — 120min超时说明问题 | ci.yml 全文 |

---

## 4. 评分汇总

| 维度 | 评分 | 评语 |
|:----|:----:|:------|
| 构建配置质量 | **2/5** | 单体CMake+大量重复+架构混合 |
| 依赖管理 | **2/5** | Gradle预发布版，ncnn默认master分支 |
| CI 设计 | **2/5** | Windows PR不触发，无缓存，E2E掩盖失败 |
| 跨平台支持 | **2/5** | 编译宏泄漏，src/win/被Android引用 |
| 构建性能 | **2/5** | 无CI缓存，无rk_core库 |
| 可重现性 | **3/5** | pnpm-lock.yaml好，Gradle validateUrl好，C++无哈希验证 |

### 总体评分：**中（2.3/5 = 4.0/10）**

---

## 5. 推荐改进

**P0**: CORE_SOURCES → rk_core 静态库；修复 foreach 循环作用域
**P1**: 提取 OpenCV 链接列表变量；修复 CI Windows PR 门控
**P1**: 配置 CI 构建缓存(ccache+Gradle)
